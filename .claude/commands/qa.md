# Command: /qa

Uruchamia agenta w roli QA.

**Workflow:**
1. Sprawdza kod przeciwko `docs_summary` oraz logice biznesowej.
2. Odpala testy (jeśli to możliwe) lub analizuje je statycznie.
3. Generuje raport Pass/Fail i ewentualną pętlę poprawek wracającą do `/impl`.
