# Command: /arch

Uruchamia agenta w roli Architecta.

**Workflow:**
1. Automatycznie wywołuje skill `read-docs`.
2. Analizuje kontekst i cel biznesowy.
3. Przygotowuje decyzje architektoniczne.
4. Uruchamia skill `handoff` przekazując dane dla `/impl` lub `/ux`.
